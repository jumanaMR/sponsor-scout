# Everything needed to run the notebook, the Streamlit app, and CV loading.
pandas>=2.0
scikit-learn>=1.3
numpy>=1.24

# Streamlit app (app.py)
streamlit>=1.30

# CV loading — .pdf and .docx (plain .txt needs neither)
pypdf>=3.17
python-docx>=1.1

# Notebook
jupyter>=1.0

# API service (sponsor_scout/api.py)
fastapi>=0.110
uvicorn>=0.27

# Optional: neural embeddings (sponsor_scout/embeddings.py). Not installed
# by default — TF-IDF+SVD works with nothing beyond the core deps above.
# pip install sentence-transformers>=2.7
